# Advent of Code 2025 OCaml

Tiny Dune project for OCaml exercises.

```sh
opam exec -- dune exec day1
opam exec -- dune build
opam exec -- dune runtest
opam exec -- dune fmt
```

If your shell has opam setup loaded, you can omit `opam exec --`.
